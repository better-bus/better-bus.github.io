import { SchoolsComponent } from './schools';

describe('SchoolsComponent', () => {
  it('should create', () => {
    const component = new SchoolsComponent();
    expect(component).toBeTruthy();
  });
});
